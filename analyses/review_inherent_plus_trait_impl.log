
✓ All structs use trait impl only (no inherent+trait duplication)!

✓ All structs use trait impl only (no inherent+trait duplication)!

✓ All structs use trait impl only (no inherent+trait duplication)!

✓ All structs use trait impl only (no inherent+trait duplication)!

✓ All structs use trait impl only (no inherent+trait duplication)!
